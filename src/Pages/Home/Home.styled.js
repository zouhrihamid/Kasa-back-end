import styled from 'styled-components';

const HomeWrapper = styled.div`
      display: felx;
      justify-content: center;
`;
const CardContainer = styled.div``;

export { CardContainer, HomeWrapper };
